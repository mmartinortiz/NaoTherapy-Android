<?xml version="1.0" encoding="utf-8"?>

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="@dimen/button_padding"
    android:layout_marginBottom="@dimen/action_top_margin"
    android:layout_marginTop="@dimen/action_top_margin"
    style="@style/sentence">

    <CheckedTextView
        android:id="@+id/conversations_sentence"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:drawableStart="@drawable/tts_emotion_normal_small"
        android:gravity="start|center_vertical"
        android:text="Dummy Text"
        android:textSize="@dimen/text_small" />

    <TextView
        android:id="@+id/sentence_edit_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_weight="0"
        android:layout_gravity="end|center_vertical"
        android:background="@android:drawable/ic_menu_edit" />
</LinearLayout>